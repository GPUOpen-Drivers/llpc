; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --tool cross-module-inline --version 3
; RUN: cross-module-inline %s %S/inc/link-fold-const.ll --link inline_fun | FileCheck %s
;
; Check that copying globals still works if they are folded into later instructions.

@glob = external global float

declare i32 @inline_fun()

define i32 @main() {
; CHECK-LABEL: define i32 @main() {
; CHECK-NEXT:    br label [[RET:%.*]]
; CHECK:       ret:
; CHECK-NEXT:    ret i32 ptrtoint (ptr @"glob.cloned.{{.*}}" to i32)
;
  %result = call i32 @inline_fun()
  br label %ret

; ret instruction is in a different basic block
ret:
  ret i32 %result
}
